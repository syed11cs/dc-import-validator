# Pipeline step registry (pipeline/registry.yaml)
PyYAML>=6.0

# Minimal requirements for running import_validation
# (from datacommonsorg/data/tools/import_validation)
absl-py
pandas
duckdb>=0.9.0,<1.5.0  # upstream runner.py passes 0-column differ DataFrame; 1.5+ rejects it
omegaconf

# Optional: for Gemini Review (scripts/llm_schema_review.py)
google-genai

# Optional: for storing reports in GCS (set GCS_REPORTS_BUCKET to enable)
google-cloud-storage

# Required by tools/import_differ/import_differ.py (top-level import; needed even for local runner mode)
google-api-python-client

# Required by util/{counters,file_util,config_map}.py and tools/statvar_importer/{mcf_file_util,data_sampler,mcf_diff}.py
# which are transitively imported at module load time by validator.py via validator_goldens.py
# (upstream commit c278165c added these unconditional top-level imports to validator.py).
psutil
gspread
chardet
retry
